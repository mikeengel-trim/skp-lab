# scripts

Shared tooling usable by any extension in this repo (e.g. test harnesses).
Empty for now — add scripts here as cross-extension needs come up, rather
than duplicating them per extension.

Extensions load straight from this GitHub repo (see
[../docs/CONVENTIONS.md](../docs/CONVENTIONS.md)), so there's no zip-packaging
script to maintain here.
