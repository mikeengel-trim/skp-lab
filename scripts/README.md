# scripts

Shared tooling usable by any extension in this repo (e.g. zipping an
extension folder for distribution). Empty for now — add scripts here as
cross-extension needs come up, rather than duplicating them per extension.
